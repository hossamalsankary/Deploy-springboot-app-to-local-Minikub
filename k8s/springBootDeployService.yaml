apiVersion: v1
kind: Service
metadata:
  labels:
    app: spring-deploy
  name: spring-service
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: spring-deploy
  type: NodePort
status:
  loadBalancer: {}
